//
//  AddTaskViewController.swift
//  GoodList
//
//  Created by Mohammad Azam on 2/27/19.
//  Copyright © 2019 Mohammad Azam. All rights reserved.
//

import Foundation
import UIKit
import RxSwift

class AddTaskViewController: UIViewController {
/*//MARK: - SUBJECTS
    //Can emmit and subscribe
    private let taskSubject = PublishSubject<Task>()
    //Property
    var taskSubjectObservable: Observable<Task> {
        return taskSubject.asObservable()
    }
    

//MARK: - OUTLETS
    @IBOutlet weak var prioritySegmentedControl: UISegmentedControl!
    @IBOutlet weak var taskTitleTextField: UITextField!
    
    
//MARK: - ACTIONS
    @IBAction func save() {
        //Приоритет выбирается из энума по номеру выбранной ячейки
        guard let priority = Priority(rawValue: self.prioritySegmentedControl.selectedSegmentIndex),
            //Заголовок
            let title = self.taskTitleTextField.text else {
                return
        }
        //Задача
        let task = Task(title: title, priority: priority)
        //Pass Task to the Subject
        taskSubject.onNext(task)
        //Закрываем контроллер
        self.dismiss(animated: true, completion: nil)
    }*/
    
}
